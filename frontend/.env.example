# Production shared backend (Recommended for team data synchronization)
VITE_API_BASE_URL=https://event-manager-api-aeyc.onrender.com

# Local standalone backend (Only if running FastAPI locally on port 8000)
# VITE_API_BASE_URL=http://localhost:8000

# Backward-compatible fallback for older local setups
# VITE_API_URL=https://event-manager-api-aeyc.onrender.com
